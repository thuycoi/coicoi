<%= form_for(word) do |f| %>
  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= simple_form_for @word do |f| %>
    <%= f.input :body, label: "Wort" %>
    <%= f.input :wordclass_id %>
    <%= f.input :definition %>
    <%= f.input :example %>
    <%= f.input :synonym %>
    <%= f.input :language_id %>
    <%= f.input :note %>
    <%= f.button :submit %>
  <% end %>

<% end %>
